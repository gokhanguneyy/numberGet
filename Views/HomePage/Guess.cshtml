@* @model numberGet.Models.GameModels.GameModel

@{
    Layout = null;
} *@

@* <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Number Guess Game</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .game-container {
            max-width: 400px;
            background: white;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
    </style>
</head>
<body>

    <div class="game-container">
        <h2 class="mb-3">Sayı Tahmin Oyunu</h2>
        <p class="fw-bold">Seçilen Mod: <span class="text-primary">@Model.SelectedLevel</span></p>
        <p class="fw-bold">Kalan Hak: <span class="text-danger">@Model.RemainingAttempts</span></p>

        @if (!Model.IsGameOver)
        {
            <form method="post">
                <div class="mb-3">
                    <label for="guess" class="form-label">Tahmininiz:</label>
                    <input type="number" id="guess" name="UserGuess" class="form-control" placeholder="Bir sayı girin" required>
                </div>

                <button type="submit" class="btn btn-primary w-100">Tahmin Et</button>
            </form>
        }

        @if (!string.IsNullOrEmpty(Model.Message))
        {
            <p class="mt-3 alert alert-info">@Model.Message</p>
        }

        @if (Model.IsGameOver)
        {
            <a href="@Url.Action("Home", "HomePage")" class="btn btn-success w-100 mt-3">Yeniden Oyna</a>
        }
    </div>

</body>
</html> *@
